server:
  port: 10086

  servlet:
    context-path: /api
spring:
  mail:
      host: smtp.qiye.aliyun.com
      port: 587
      username: oschip.cloud@bosc.ac.cn
      password: kxy@986Bosc&
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB
  datasource:
    url: jdbc:mysql://172.18.20.81:3306/tbPROD?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: 172.18.20.81
    port: 6379

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl



sa-token:
  # token 名称（同时也是 cookie 名称）
  token-name: token
  # token 有效期（单位：秒） 默认30天 2592000，-1 代表永久有效
  timeout: 2592000
  # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
  active-timeout: -1
  # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）  允许同一账号在多个设备一起登录
  is-concurrent: false
  # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）   同一账号每次登录产生不同的token
  is-share: false
  # token 风格（默认可取值：uuid、simple-uuid、random-32、random-64、random-128、tik）
  is-read-header: true
  token-style: jwt
  # 是否输出操作日志
  is-log: true
  # jwt秘钥
  jwt-secret-key: hhhhhhhhhhhhhhhhhhhhhh
  # 配置Sa-Token单独使用的Redis连接 （此处需要和SSO-Server端连接同一个Redis）
#  alone-redis:
#    # Redis数据库索引 (默认为0)
#    database: 0
#    # Redis服务器地址
#    host: 172.18.20.81
#    # Redis服务器连接端口
#    port: 6379
#    # Redis服务器连接密码（默认为空）
##    password: root
#    # 连接超时时间
##    timeout: 10s

minio:
  endpoint: http://172.18.20.81:9000
  access-key: admin
  secret-key: fhl3yjsdw$
  bucket: attachments-tb

minio-prefix: https://www.oschip.cloud:8126/minio
